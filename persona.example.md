# persona.md 寫法說明

`persona.md` 是**機器人的角色設定檔**。記帳完成後，`gemini.py` 會把這個檔案的**全文**當成
system prompt 丟給 Gemini，再附上剛剛那筆帳的摘要，請它回一句話。

這個檔案（`persona.example.md`）只是說明文件。**要讓角色生效，請自己建立 `persona.md`**——
它已經在 `.gitignore` 裡，不會進版控，所以你可以放任何私人的、玩梗的設定。

```bash
cp persona.example.md persona.md   # 然後把內容整份換掉，照下面的原則寫
```

沒有 `persona.md` 時，機器人會退回讀這個檔（也就是不會有角色，只會照著這份說明講話），
功能不受影響。

## 它實際上怎麼被用

```
persona.md 全文
+ 「主人剛剛完成了一筆記帳，以下是帳目摘要：{摘要}」
+ 「請根據你的角色設定，對這次的記帳內容做出簡短、有趣的回應。」
        ↓
    Gemini → 一句話 → 接在記帳成功訊息後面回給使用者
```

同一份 persona 也被週報／月報的 AI 評語共用（走 codex CLI），所以寫的時候要考慮
「這個角色講長篇評語會是什麼樣子」。

## 寫得好的四個原則（都是踩過坑換來的）

### 1. 判斷依據要給明確的規則，不要讓它自己猜

**反例**：只寫「看到大筆支出要吐槽」。模型手上只有一筆帳的金額，於是它唯一能用的判斷
就是數字大小——結果每筆稍微大一點的都被唸，就算你根本沒有超支。

**正解**：把判斷依據寫成表格，並且**明確定義「資料不足時該怎麼辦」**。例如：

```markdown
如果摘要裡沒有預算/餘額資訊：
→ 預設使用者沒有超支。
→ 嚴禁講「你又亂花錢」「這樣下去會沒錢」這類沒有根據的告誡。
→ 只能做三件事：捧場、吐槽品項本身、講角色自己的廢話。
```

沒有這段的話，模型會用想像力填補空白，而它的想像力預設是「碎唸」。

### 2. 不要給「口頭禪清單」

這是最反直覺的一條。列一份「經典台詞／發語詞」清單，感覺會讓角色更像，
**實際上會讓它每一則都從那五句裡抽**，兩天就膩。

改成：給幾句範例當**語感示範**，然後明講「這是要你抓節奏，不是給你抄」，
再把角色的說話方式**歸納成法則**（例：「愛面子，被戳破就硬凹」「常常講到一半自我拆台」）。
法則能生出無限變化，清單只能生出五句。

### 3. 明確要求變化

模型預設會收斂到同一種句型。要對抗這點就得寫死規則：

```markdown
- 不要有固定開場白。
- 同一個梗七天內不要用第二次（包含比喻、綽號、結尾語）。
- 每則只挑一個角度講，不要每則都想把身分、情緒、建議全塞進去。
- 長度要變化：多數 20–50 字，但允許偶爾只回四五個字。
```

另外給角色一個**私人素材庫**（它自己的生活、煩惱、怕的東西），
它就有東西可以扯，不會每次都繞回「你花錢了」。

### 4. 講清楚輸出格式

這個回應是直接貼進 LINE / Discord 的，所以：

```markdown
- 純文字，20–50 字為主。
- 不要 markdown、不要列點、不要標題。
- 不要每則都放 emoji；要放就一個。
- 只輸出角色要講的那句話，不要任何前言或說明。
```

沒有最後一行的話，模型很愛回「好的，這是我的回應：……」。

## 建議的檔案骨架

```markdown
# 系統角色設定：<角色名>

## 核心身分
<它是誰、跟使用者什麼關係、目標是什麼>
<一句話定調：它是搭檔還是監工？>

## 講話的聲音
<3-8 句語感範例 + 明講「學節奏不要抄」>
<從範例歸納出的說話法則>

## 判斷依據
<用表格定義什麼情況該有什麼反應>
<★ 資料不足時的預設行為 ★>

## 反重複規則
<不要固定開場白／同梗不重複／每則只一個角度／長度要變化>

## 私人素材庫
<角色自己的生活細節，拿來變花樣>

## 輸出格式
<長度、純文字、不要前言>
```

## 注意事項

- **改完要重啟才生效**：`persona.md` 是在 `gemini.py` import 時一次讀進 `PERSONA_TEXT`，
  改檔案不會熱更新。跑 `docker restart money-bot`。
- **角色設定會連同帳目摘要送給第三方 AI 服務**（Gemini / OpenAI），不要在裡面寫真實個資。
- **失敗會靜默降級**：AI 呼叫失敗時 `generate_persona_comment()` 回空字串，
  記帳本身照常成功，只是沒有角色回應。
